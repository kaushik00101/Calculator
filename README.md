# Calculator
A responsive, grid-based web calculator application built using vanilla HTML5, CSS3, and JavaScript. Features a dedicated evaluation engine that handles real-time user inputs, floating-point math, and inline string corrections via a custom delete (del) handler. Designed with a mobile-friendly layout and hosted live on GitHub Pages.
